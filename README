To run this application you need to follow following steps:1.First Create a schema using query given below    CREATE DATABASE user_data;2.Change the application properties -> change user name and password accordingly.3. Run the AssignmentApplication class to create tables.4.See the controller class to perform operation.