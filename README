To run this application you need to follow following steps:----To deploy on EC2 ----1.First Create a schema using query given below    CREATE DATABASE user_data;2.Change the application properties -> change user name and password accordingly.3.use command git clone https://github.com/shs2147/assignment.git4.go inside the folder.5.Run mvn install -DskipTests6.then use this cmd to start: mvn spring-boot:run7.See the controller class to perform operation.